import { CombineReasonsFeedbackPipe } from './combine-reasons-feedback.pipe';

describe('CombineReasonsFeedbackPipe', () => {
  it('create an instance', () => {
    const pipe = new CombineReasonsFeedbackPipe();
    expect(pipe).toBeTruthy();
  });
});
